import pygame
from pygame.locals import *
from .enemy import Enemy

class Slow(Enemy):
    def __init__(self, pos, target):
        super().__init__(pos)
        self.target = target
        self.vel = 1
